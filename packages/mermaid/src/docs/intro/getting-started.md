---graph TD
    subgraph Milano
        direction LR
        A[STORIA] --> B(MILANO NELLA SECONDA GUERRA MONDIALE)
        C[GEOGRAFIA] --> D(LE FILIPPINE)
        E[MUSICA] --> F(LA SCALA)
        G[EDUCAZIONE FISICA] --> H(INTER E MILAN)
        I[EDUCAZIONE CIVICA] --> J(EXPO 2015)
        K[ITALIANO] --> L(IL FUTURISMO)
        M[SCIENZE] --> N(LE SCOPERTE DI ALBERT EINSTEIN)
        O[ARTE] --> P(BOCCIONI E IL MUSEO DEL '900)
        Q[TEDESCO] --> R(ALBERT EINSTEIN)
        S[INGLESE] --> T(NORMAN FOSTER)
        U[TECNOLOGIA] --> V(LA CENTRALE SANTA RADEGONGA)
    end
## Closing note

```note
Comments from Knut Sveidqvist, creator of Mermaid:

- In early versions of Mermaid, the `<script>` tag was invoked in the `<head>` part of the web page. Nowadays, we can place it in the `<body>` as seen above. Older parts of the documentation frequently reflect the previous way, which still works.
```
